package com.jgames.survival.model.api.gameload;

public interface JsonApiConstants {
    interface ResourceNamespaces {
        String CORE_RESOURCES = "core";
    }

    interface Attributes {
        String CLASS_PATH = "classPath";

        interface BattleModel {
            String BATTLE_OBJECT_TYPE = "battleModelType";
        }
    }
}
